{# j2lint: disable=jinja-variable-format #}
### Autogenerated by smokeping.py ###
#
# select master or slave mode
MODE=slave
# in master mode, the rest of the configuration is in
# /etc/smokeping/config
#
# in slave mode, uncomment and set the following variables too
# see smokeping(1)
#
# Mandatory configuration
MASTER_URL={{ instrumentation_url }}
SHARED_SECRET=/etc/smokeping/clientsecrets.conf
#
# Optional configuration
SLAVE_NAME={{ client_name }}

{% if vrf is vyos_defined %}
VRF_ARGS="ip vrf exec {{ vrf }}"
{% else %}
VRF_ARGS=""
{% endif %}

