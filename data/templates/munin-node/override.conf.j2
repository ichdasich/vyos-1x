{% set vrf_command = '/usr/bin/ip vrf exec ' ~ vrf ~ ' ' if vrf is vyos_defined else '' %}
[Unit]
After=
After=vyos-router.service

[Service]
ExecStart=
ExecStart={{ vrf_command }}/usr/sbin/munin-node --foreground $DAEMON_ARGS
RestartSec=10
